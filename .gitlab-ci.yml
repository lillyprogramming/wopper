stages:
  - deploy

pages:
  stage: deploy
  image: alpine:latest
  tags: ["docker"]
  script:
    - echo "Deploying GitLab Pages from ./pages"
    - ls -la pages
  artifacts:
    paths:
      - pages
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'